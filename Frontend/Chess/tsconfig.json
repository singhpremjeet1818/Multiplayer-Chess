{
  "compilerOptions": {
    "baseUrl": "./src",        // Set the base directory for imports (relative to src)
    "paths": {
      "@/*": ["./*"]           // Define the alias for the @ symbol
    },
    "types": ["node"],         // Include node types for core modules like 'path'
    "jsx": "react-jsx"         // Enable JSX transformation (for React)
  },
  "include": [
    "src/**/*"
  ]
}
